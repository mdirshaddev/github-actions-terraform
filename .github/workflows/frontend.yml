name: Frontend Workflow

on:
  push:
    branches:
      - main
    paths:
      - ./frontend

jobs:
  deploy_job:
    name: deploy job
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: "Say Hello World"
        run: |
          echo "Say, Hello World from Frontend"
